#include "main.h"

/**
 * _isdigit - chscks for a digit (0 through 9)
 * @c: int the checked
 * Return: 1 if c is a digit, 0 otherwise
 * By Rania Hamouda
 */

int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
