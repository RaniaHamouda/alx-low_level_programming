#include "main.h"

/**
 * _isupper - checars for uppercase chacter
 * @c: the character to be checked
 * Return: 1 if c is uppercase, 0 otherewise
 */

int _isupper(int c)
{
	return (c >= 'A' && c <= 'Z');
}
